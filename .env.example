# Vite Environment Variables
# Leave empty to use same-origin /api via proxy (recommended for Docker).
VITE_API_URL=

# Development/testing only: set true to match backend BYPASS_TASK_TIMING_GATE.
VITE_BYPASS_TASK_TIMING_GATE=false
